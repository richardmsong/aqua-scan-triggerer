name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  claude-review:
    uses: richardmsong/shared-workflows/.github/workflows/claude-code-review.yml@main
    secrets: inherit
